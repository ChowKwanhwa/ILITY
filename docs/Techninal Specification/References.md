# References

- PLONK-based ZK verification layer on EVM (no zkEVM fork)
- EIP-1108 / 4844 for gas & data efficiency (4844 = data availability, not on-chain storage)
- zk-bridging of L2 → ILITY Mainnet
- DID·ENS·IPFS identity clustering & anonymous querying
- Recursive multi-chain ZK asset aggregation
- KZG/BLS12-381 handled via consensus DA or recursive/off-chain paths (no L1 precompile yet)
- Bridge security inherits AMB/LayerZero trust models; payloads must be threat-modeled
